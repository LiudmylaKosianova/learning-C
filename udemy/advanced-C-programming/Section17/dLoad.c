#include <stdio.h>
#include <stdlib.h>
#include <dlfcn.h>

int main(){

    void *handle = NULL; //for handling dlopen
    double (*cosine)(double) = NULL; //function cosine() from math library
    char *error = NULL; //for derror
    

    return 0;
}